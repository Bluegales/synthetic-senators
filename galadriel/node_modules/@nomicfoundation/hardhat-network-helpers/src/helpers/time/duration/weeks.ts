import { days } from "@nomicfoundation/hardhat-network-helpers/src/helpers/time/duration/days";

/**
 * Converts weeks into seconds
 */
export function weeks(n: number): number {
  return days(n) * 7;
}
