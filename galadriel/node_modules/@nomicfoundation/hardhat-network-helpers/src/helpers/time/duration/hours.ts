import { minutes } from "@nomicfoundation/hardhat-network-helpers/src/helpers/time/duration/minutes";

/**
 * Converts hours into seconds
 */
export function hours(n: number): number {
  return minutes(n) * 60;
}
