import { hours } from "@nomicfoundation/hardhat-network-helpers/src/helpers/time/duration/hours";

/**
 * Converts days into seconds
 */
export function days(n: number): number {
  return hours(n) * 24;
}
